# 360toolkit - Requirements
# Unified photogrammetry preprocessing pipeline

# ============================================================================
# CORE DEPENDENCIES (Required)
# ============================================================================
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
piexif>=1.1.3

# ============================================================================
# GUI FRAMEWORK
# ============================================================================
PyQt6>=6.5.0
PyQt6-Qt6>=6.5.0

# ============================================================================
# AI MASKING - Choose ONE option:
# ============================================================================

# OPTION A: ONNX Runtime (RECOMMENDED for production)
# Lightweight, fast, ~300 MB binary size
onnxruntime>=1.20.0

# For GPU acceleration (optional):
# pip install onnxruntime-gpu>=1.20.0

# OPTION B: PyTorch + Ultralytics (INSTALLED - for full YOLOv8 support)
# Required for YOLOv8 masking with model auto-download
ultralytics>=8.0.200

# PyTorch with CUDA 12.x support (RTX 30/40/50 series + GTX compatibility)
# For RTX 50-series (Blackwell sm_120), use PyTorch nightly or wait for official 2.7+
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
torchvision>=0.20.0

# IMPORTANT: If you have RTX 50-series GPU, you need PyTorch nightly build:
# pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu124

# ============================================================================
# EXTERNAL DEPENDENCIES (Install separately)
# ============================================================================
# - FFmpeg: Download from https://ffmpeg.org/download.html
# - Insta360 MediaSDK 3.0.5: Windows only, bundled in portable release
#   or download from https://www.insta360.com/sdk/home

# ============================================================================
# DEVELOPMENT (Optional)
# ============================================================================
# pytest>=7.4.0
# pytest-cov>=4.1.0
