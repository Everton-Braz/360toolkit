# 360FrameTools - Requirements (OPTIMIZED)
# Complete photogrammetry preprocessing pipeline
# OPTIMIZATIONS:
# - OpenCV kept minimal (required for cv2.remap in transforms + video metadata)
# - PyTorch CPU-only by default (reduces size by ~1.5GB)
# - Removed torchvision (not used in codebase)
# - Pillow for basic image I/O (lighter alternative where possible)

# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0  # Required for cv2.remap (transforms) and VideoCapture (metadata)
Pillow>=10.0.0  # Lighter image I/O alternative

# PyQt6 for GUI
PyQt6>=6.5.0
PyQt6-Qt6>=6.5.0

# AI/ML for masking (Stage 3 only)
ultralytics>=8.0.200  # YOLOv8 instance segmentation

# PyTorch - CPU-ONLY VERSION (reduces binary size by ~1.5GB)
# For CPU-only (recommended for smaller binary):
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu

# For GPU acceleration (larger binary, ~2GB+), use instead:
# pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118

# REMOVED: torchvision (not used in codebase - saves ~500MB)

# Metadata handling
piexif>=1.1.3

# Required external tools (not Python packages):
# - FFmpeg: Required for Stage 1 (frame extraction). Install separately.
#   Windows: Download from ffmpeg.org
#   Linux: apt install ffmpeg
#   macOS: brew install ffmpeg
# - Insta360 SDK: Windows only, required for .insv stitching (Stage 1)
#   Copy from Original_Projects folder

# Development dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
